# PHASES: mcp-proxy

## Active

- phase: 5
  id: polish
  title: "Fejlhåndtering og polish"
  milestone: "Produktionsklar proxy"
  delivers:
    - Crash-recovery (downstream dies → remove tools → list_changed)
    - Graceful shutdown (SIGTERM handling)
    - Configurable timeout (make 5s tunable)
    - Error resilience
  status: planning

# --- DONES ---

- phase: 4
  id: config-and-restart
  title: "Config file + restart + watcher + logging + compartmentalization"
  milestone: "Auto-load servere fra config, restart ved crash/binary-ændring, struktureret logging, compartmentaliseret tool-liste"
  delivers:
    - mcp-servers.json config parser
    - Auto-load gui-mcp, rules, rulestools ved proxy start
    - mcp/load, mcp/unload, mcp/list, mcp/restart management tools
    - Binary file watcher (notify crate)
    - Auto-restart ved binary Modify event
    - Structured logging (tracing crate → mcp-proxy.log)
    - 5-second initialization timeout for slow Python servers
    - Compartmentalized mcp/list output (tools grouped by server_id)
  status: done

- phase: 1
  id: setup
  title: "Project setup"
  milestone: "Project scaffolded, builds, scans clean"
  delivers:
    - Project structure
    - Build configuration (tokio, notify, serde)
    - Scanner integration
  status: done

- phase: 3
  id: mcp-protocol
  title: "MCP protocol + multiplexer"
  milestone: "Proxy er selv en MCP-server, kan spawne og route til multiple downstream servere"
  delivers:
    - JSON-RPC parser (upstream read loop)
    - MCP initialize/tools_list/tools_call handlers
    - Downstream server registry (spawn, initialize, collect tools)
    - Tool routing: namespace prefix → downstream dispatch
    - mcp/load, mcp/unload, mcp/list, mcp/restart management tools
    - notifications/tools/list_changed upstream
  status: done

- phase: 2
  id: proxy-core
  title: "Stdio relay + supervisor"
  milestone: "Proxy relay'er stdin/stdout mellem Claude og downstream process"
  delivers:
    - ProcessGateway_gtw (spawn downstream)
    - RelayGateway_gtw (tokio::io::copy)
    - ProxyAdapter_adp (dual relay with tokio::join)
    - CLI arg parsing (binary path + args)
  status: done
